# --- Databáze (PostgreSQL lokálně i na Vercelu) ---
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/gamecritic"

# NextAuth — vygenerujte: openssl rand -base64 32
AUTH_SECRET="nahradte-nahodnym-retezcem"

# Veřejná URL webu (metadata, sitemap, canonical)
NEXT_PUBLIC_SITE_URL="http://localhost:3000"

# Google Analytics 4 — volitelné; načte se až po souhlasu s analytikou
NEXT_PUBLIC_GA_MEASUREMENT_ID=""

# OAuth GitHub (volitelné)
GITHUB_ID=""
GITHUB_SECRET=""
